# Direct dependencies with compatible-release bounds.
# Exact resolved versions are captured in requirements.lock.txt.

# Web framework and extensions
Flask~=3.1
Flask-SQLAlchemy~=3.1
Flask-Migrate~=4.0
Flask-JWT-Extended~=4.7
Flask-Cors~=5.0
Flask-Limiter~=3.9

# Database
SQLAlchemy~=2.0
psycopg[binary]~=3.2

# Validation
pydantic[email]~=2.10

# Security
argon2-cffi~=23.1

# Configuration
python-dotenv~=1.0

# Testing
pytest~=8.3
pytest-cov~=6.0
